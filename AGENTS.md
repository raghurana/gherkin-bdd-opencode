## Project Structure

- `src/`: Contains all the source code for the project.
- `docs/`: Contains all the documentation for the project.
- `features/`: Contains all the feature files.

## Testing Guidelnes

- Do not stop until `npm run validate` is successful.